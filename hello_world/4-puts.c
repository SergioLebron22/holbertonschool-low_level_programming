#include <stdio.h>

/**
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char sring[] = "\"Programming is like building a multilingual puzzle \n";
	puts(string);	
	return (0);
}
